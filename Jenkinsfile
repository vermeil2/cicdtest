pipeline {
    agent any
    tools {
        maven 'maven_3.9.9'
    }
    stages {
        stage('build') {
            steps {
                sh "mvn clean package"
            }
        }
        stage('docker build'){
            steps {
                sh "
            }
        }
    }
}

